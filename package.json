{
  "name": "pepstep-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "PepStep — Diary, Workouts, and More. Put a Pep in Your Step.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check-data": "node scripts/check-data.js"
  },
  "devDependencies": {
    "vite": "^6.3.6"
  }
}
